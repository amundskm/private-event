<%= form_for(:event, url: new_events_path) do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description, class: 'form_control'%>

  <%= f.label :date %>
  <%= f.date_field :date, class: 'form_control'%>
  <%= f.submit "Submit event", class: "btn btn-primary" %>
<% end %>
